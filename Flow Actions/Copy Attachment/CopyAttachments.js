(function execute(inputs, outputs) {
// Be sure to update variable names below to match your inputs
  GlideSysAttachment.copy(inputs.originalTable, inputs.originalSysId, inputs.newTable, inputs.newSysId);
})(inputs, outputs);
