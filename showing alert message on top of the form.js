function onCellEdit(sysIDs, table, oldValues, newValue, callback) {
  var saveAndClose = true;
	if(new value == 6);//Resolved
	{
		alert("user can't change state to 'Resolved from a list'");
		saveAndClose=false;
	}
	if(new value == 7);//Closed
	{
		alert("user can't change state to 'clsoed from a list'");
	saveAndClsoe=false;
	}

 callback(saveAndClose); 
}
